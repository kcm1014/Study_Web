length
charAt(n)                // n번째 문자
charCodeAt(n)            // n번재 문자 코드값
concat(str)              // 문자열 뒤쪽에 str 연결
indexOf(substr)
lastIndexOf(substr)
match(reg)                //정규 표현식 검사
replace(reg,rep)
search(reg)
slice(start, end)
split(str)   // str로 분할해서 배열 생성
substr(start[,count])
toLowerCase()
toUpperCase()
tirm()
