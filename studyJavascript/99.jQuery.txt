$() : $함수 호출 파라메타로 전달된 노드를 찾아주는 역할
let li = $("menu1 li");


이벤트 등록
$대상.on("이벤트 이름", 이벤트 리스너);
$("#btnCheck").on("click",function(){
    alert("환영합니다!");
});


스타일 설정하기
$대상.css("스타일 이름", 값);
$대상.css({
    "스타일 이름":값,
    "스타일 이름":값,...
})